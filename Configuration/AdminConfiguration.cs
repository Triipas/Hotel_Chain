// Configuration/AdminConfiguration.cs
namespace Hotel_chain.Configuration
{
    public static class AdminConfiguration
    {
        public static IServiceCollection ConfigureAdminServices(this IServiceCollection services)
        {
            // Servicios específicos para el panel admin
            // Por ahora no hay servicios específicos, pero aquí se agregarían
            
            return services;
        }
    }
}