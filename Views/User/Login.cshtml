@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Inicio de Sesión - Cliente";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/client/css/pages/login.css">
</head>
<body>
    <div class="login-wrapper">
        <div class="login-left"></div>
        <div class="card" id="mainCard">
        <div class="card-inner">
            <div class="front">
                <h2>Iniciar Sesión</h2>

                @* Mensaje de error *@
                @if (ViewBag.Mensaje != null)
                {
                    <div class="error-message">@ViewBag.Mensaje</div>
                }

                <form id="loginForm" method="post" asp-controller="Login" asp-action="LoginUser">
                    <div class="form-group"> 
                        <label for="email">E-mail</label>
                        <input type="email" id="email" name="email" placeholder="Ingrese su E-mail" required>
                    </div>
                    <div class="form-group"> 
                        <label for="contra">Contraseña</label>
                        <input type="password" id="contra" name="contra" placeholder="Ingrese su Contraseña" required>
                    </div>
                    <br>
                    <button type="submit" class="btn-login">Iniciar Sesión</button>
                    <a href="#" id="showRegister" class="btn-register">Crear una cuenta</a>
                    
                    <div style="margin-top: 20px; padding-top: 20px; border-top: 1px solid #e0e0e0; text-align: center;">
                        <p style="font-size: 0.9rem; color: #666; margin-bottom: 10px;">
                            ¿Eres parte del equipo?
                        </p>
                        <a href="/Admin/Login" style="color: #667eea; text-decoration: none; font-weight: 500;">
                            Acceder al Panel de Administración →
                        </a>
                    </div>
                </form>
                <div class="back-link">
                    <a href="/">← Volver al sitio principal</a>
                </div>
            </div>

            <div class="back">
                <div class="back1">
                    <h2>Registro de Usuario</h2>

                    @if(ViewBag.Mensaje != null)
                    {
                        <div class="error-message">@ViewBag.Mensaje</div>
                    }
                    
                    <form id="registerForm" method="post" asp-controller="Login" asp-action="RegisterUser">
                        <label>Nombre</label>
                        <input type="text" name="nombre" id="regNombre" required>
                        
                        <label>Apellido</label>
                        <input type="text" name="apellido" id="regApellido" required>
                        
                        <label>E-mail</label>
                        <input type="email" name="email" id="regEmail" required>
                        
                        <label>Documento (DNI/Pasaporte)</label>
                        <input type="text" name="documento" id="regDocumento" required 
                               placeholder="Ej: 12345678">
                        
                        <label>Teléfono</label>
                        <input type="text" name="telefono" id="regTelefono" required>
                        
                        <label>Contraseña</label>
                        <input type="password" name="contra" id="regContra" required>
                        
                        <button type="submit" id="registerBtn">Registrar</button>
                    </form>
                    <a href="#" id="showLogin" class="btn-login">Ya tengo una Cuenta</a>
                </div>
                <div class="back2">
                    <div class="success-icon">✓</div>
                    <h2>¡Cuenta registrada exitosamente!</h2>
                    <p>Tu cuenta ha sido creada correctamente. Ya puedes iniciar sesión con tus credenciales.</p>
                    <a href="#" id="returnToLogin" class="return-login">Iniciar Sesión</a>
                </div>
            </div>
        </div>
        </div>
    </div>

    <script src="~/client/js/client-login.js"></script>

</body>
</html>